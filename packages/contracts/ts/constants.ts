/**
 * Enum representing request type
 */
export enum ERegistryManagerRequestType {
  Add,
  Change,
  Remove,
}

/**
 * Enum representing request status
 */
export enum ERegistryManagerRequestStatus {
  Pending,
  Approved,
  Rejected,
}
